/**
 * Mobile search is now handled entirely within sheet-content.js.
 * This module is kept as a no-op stub for import compatibility.
 */

export function initMobileSearch() {
    // Search bar + category grid are rendered inside the bottom sheet content
}

export function clearSearch() {
    // No-op: managed by sheet-content
}
